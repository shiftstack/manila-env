all:
  hosts:
    localhost:
  children:
    devstack_host:
      hosts:
        {{ devstack_host_info.server.public_v4 }}:
          ansible_user: {{ devstack_user }}
          ansible_ssh_private_key: {{ private_key_file }}



